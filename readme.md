# PetConnect

Monorepo com frontend (React) e backend (Node/Express).

## Como rodar

### Backend
cd petfinder-api
npm install
# criar .env com chaves
node server.js

### Frontend
cd petconnect-web
npm install
npm run dev
